// Typescript type definitions for Book object.

export interface IBook {
    entry_id?: number;
    title: string;
    author: string;
    genre: string;
    publication_date: string;
    isbn: string;
}